# 环境概览

返回文档环境概览为您提供查看环境基础信息、资源使用情况和全局环境变量等相关信息。

## 添加 AK/SK

AK/SK 为调用 WebSDK 的密钥，主要用于调用 WebSDK，详情请参见常规用法。1登录云开发平台

。2在左侧导航栏单击环境管理 > 环境概览。3在环境基础信息区域单击添加。4在新建弹窗中，根据需求配置以下参数，配置完成后单击确定。1
| 字段 | 说明 |
| --- | --- |
| 状态 | 单击状态开关后，启用/关闭 AK/SK。 |
| 描述 | （必填项）请根据需求输入描述内容。 |
| 密钥生效范围设置 | 请根据实际需求配置密钥生效范围。 |


## 资源包使用情况

在资源包使用情况页签下，查看云函数、数据库、存储和静态站点等资源的使用情况。

## 全局环境变量

环境变量属于函数运行参数，可以在函数中定义，也可以在环境中定义。函数定义的环境变量只能在所定义的函数中生效，环境中定义的全局环境变量可以在此环境下的所有函数生效。1在全局环境变量页签单击编辑。2单击添加一行数据后，输入 Key 值和 Value 值。3单击保存完成全局环境变量配置。

### 示例：配置全局变量 appv 1

1您可以在此配置整个环境维度的全局变量，所有函数均可以使用该环境变量。2使用环境变量。在云函数中，可以通过 process.env["变量名称"] 使用局部变量或者全局变量。
```Plain Text
exports.main = async (event, context) => {
return "局部变量test值:" + process.env["test"] + "；全局变量appv1值:" + process.env["appv1"];
};
```
3发布函数后，通过测试函数功能查看调用结果。

## 用户指标数据

在用户指标数据区域您可以查看云函数资源使用量、调用次数和出网流量等信息。您还可以通过右上角切换时间周期查看相应数据。

## 更改套餐

1在环境概览页面右上角单击更改套餐。2在更改套餐页面，您可根据实际需求选择套餐后单击确认更改。套餐详细内容请参见套餐升级与降级。

## 套餐实例规格

1在环境概览页面右上角单击套餐实例规格。2在套餐实例规格弹窗中您可以查看实例名称、实例规格和实例 id 等相关信息。

## 销毁

1在环境概览页面右上角单击销毁。注意：●将环境整体删除时，该动作会将环境内的数据同步删除，且不可恢复，请谨慎操作。●在删除环境时，无法删除当前所在环境，您可切换到其它环境后，再删除需要删除的环境。例如，您需要删除 test 环境，请切换到其它环境后，再删除 test 环境。2在销毁弹窗中输入确认销毁后，单击我已知晓风险，确定销毁。

## 冻结

冻结后该环境下云函数、数据库、存储、静态站点和组件等相关内容均不可使用。1在环境概览页面右上角单击冻结。注意：环境冻结后仍然会产生存储费用。2在冻结弹窗中输入确认冻结后，单击我已知晓风险，确定冻结。说明：如需重新启用该环境，请在环境概览页面右上角单击复用。

## 相关文档

●如需查看新增环境或切换环境，请参见管理环境。●如需进行访问服务相关的配置，请参见访问服务。●如需配置云函数防盗刷，请参见安全配置。●如需配置开发接口服务，请参见云调用。​
