# 签名机制

返回文档WebSocket 网关对于每一个 ws 或 wss 的建连请求进行签名验证，用于阻止未经授权的连接请求。签名采用对称加密方式，在客户端和服务端各自使用 SK 对建连请求加密后进行比对。SK 为私密信息，应避免泄露并定期轮换。

# 签名格式说明

## 1. 签名请求示例


```
ws://env-00jx00000000.api-hz.cloudbasefunction.cn/ws/function/test-websocket?accessKeyId=4dE4XLpwZz9GFr0E&signature=92c72924d3175de180b3a01864ef907a17520adc9e2fa36e3077b9eeb7879c08&signatureNonce=nonce9988998899889988&signatureVersion=v1&timestamp=1715000869722&key1=val1&key2=val2
```


## 2. 请求参数说明

签名放在query string中。
| 序号 | 参数名 | 值类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| 1 | accessKeyId | string | 是 | ak，从云开发控制台获取。 | 4dE4XLpwZz9GFr0E |
| 2 | authorization | string | 否 | 云开发身份服务签发的accessToken，用于识别用户身份。 | eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c |
| 3 | signature | string | 是 | 签名，16进制字符串。 | 7650234949964e838ea0393cb0bb8185095410fd27756a1dc7dfc05462cce03d |
| 4 | signatureNonce | string | 是 | 随机字符串，用于防止重放攻击，180秒内不允许重复。长度范围[20, 56]。不要使用时间戳，因其易于重复，可使用uuid或其他随机串生成方法。 | 3b1840a6-2184-4a50-8f74-49d85ec79588 |
| 5 | signatureVersion | string | 否 | 签名版本，当前只支持"v1"，可不填。用于支持未来可能的签名算法升级。此参数用于指明签名版本，不参与签名计算。 | v1 |
| 6 | timestamp | string | 是 | 13位UNIX时间戳，请求在前后60秒内有效。 | 1715000869722 |


# 签名机制

## 1. 构造规范化请求

在进行签名前，首先需要规范请求，确保同一个请求在客户端和服务端都有相同的规划化表示，进而可以得到相同的签名结果。CanonicalRequest =  URI + '\n' + QueryString + '\n' + 'host:' + HOST1URI，包含URL中host之后（从/开始，包含/）QueryString之前（到?为止，不包含?）的部分2QueryString，需经过规范化，步骤如下a从QueryString中筛选中参与签名的参数，包括 ["accessKeyId", "authorization", "signatureNonce", "timestamp"]b使用等号'='连接请求参数名和参数值c将参数按升序排序，使用与号'&'连接3HOST，包含URL中host部分，从'ws://'或'wss://'开始到URI之前（到/为止，不包含/）的部分，最后没有换行符'\n'

#### 示例：

/ws/function/test-websocketaccessKeyId=4dE4XLpwZz9GFr0E&signatureNonce=nonce9988998899889988&timestamp=1715000869722host:env-00jx00000000.api-hz.cloudbasefunction.cn

## 2. 构造待签名字符串

StringToSign = SignatureAlgorithm + '\n' + HashedCanonicalRequest1SignatureAlgorithm，签名算法，当前只支持 HMAC-SHA2562HashedCanonicalRequest，规范化请求的摘要串，计算方法 HashedCanonicalRequest=HexEncode(Hash(CanonicalRequest))aHash，摘要算法，当前仅支持 SHA256bHexEncode，16进制编码，小写形式

#### 示例：

HMAC-SHA2566ffba6c6d53bcf694d5cb6991f034b9d56cd2840b05cbc1a0f8c23b9f8226c5f

## 3. 计算签名

Signature = HexEncode(SignatureMethod(Secret, StringToSign))1StringToSign，上一节中构造，使用UTF-8编码2Secret，签名密钥，即SK，从云开发控制台获取，需与AK(accessKeyId)匹配3SignatureMethod，签名算法，当前仅支持 HMAC-SHA2564HexEncode，16进制编码，小写形式

#### 示例：

92c72924d3175de180b3a01864ef907a17520adc9e2fa36e3077b9eeb7879c08

## 4. 签名添加到请求的query string中

将计算出的签名值，以 signature 作为参数名，与 accessKeyId、authorization、signatureNonce、signatureVersion、timestamp 等一起，加入到请求的 QueryString 中，发起建连请求。额外的用户自定义参数也加入QueryString，如示例中的 key1=val1&key2=val2 。

#### 示例：

ws://env-00jx00000000.api-hz.cloudbasefunction.cn/ws/function/test-websocket?accessKeyId=4dE4XLpwZz9GFr0E&signature=92c72924d3175de180b3a01864ef907a17520adc9e2fa36e3077b9eeb7879c08&signatureNonce=nonce9988998899889988&signatureVersion=v1&timestamp=1715000869722&key1=val1&key2=val2
```Bash
wscat -c 'ws://env-00jx00000000.api-hz.cloudbasefunction.cn/ws/function/test-websocket?accessKeyId=4dE4XLpwZz9GFr0E&signature=92c72924d3175de180b3a01864ef907a17520adc9e2fa36e3077b9eeb7879c08&signatureNonce=nonce9988998899889988&signatureVersion=v1&timestamp=1715000869722&key1=val1&key2=val2'
```
​
