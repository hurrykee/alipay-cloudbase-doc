# 新建表

返回文档本文主要介绍新建表的操作步骤。

## 操作步骤

在数据库管理页面，左侧库表资源区域选择要新建表的数据库或表分组，右键单击表名打开功能菜单，选择新建表，打开新建表页面。

#### 方法一：可视化建表

在新建表页面，配置表信息，包括基本信息、列信息、索引（可选）。注意：如需将新建表同时发布到多个库，请参见以下操作新增发布库。●只有当前账号的成员角色是账号管理员，或者有本 APP 应用管理员权限时，才可新增发布库。●如发布库已存在同名表，将对比表结构执行添加/修改列、添加/删除索引、修改表信息变更。出于安全考虑不会删除多余列。参数说明如下：
| 参数 | 说明 |
| --- | --- |
| 基本信息 | 表名 |
| 备注 | 当前表的备注信息。 |
| 字符集 | 当前表的字符集，不可更改。 |
| 排序规则 | 当前表的排序规则。 |
| 自增起始值 | 当前表的自增起始值，大于等于0。 |
| 列信息 | 列名 |
| 类型 | 类型共四大类，12小类。●数值：int、decimal、bit●日期：date、time、year●字符：char、varchar、binary、varbinary●大对象：blob、text |
| 长度 | 不填则取默认值。 |
| 默认值 | 不填则取默认值。 |
| 可空 | 可选择是否可空。 |
| 备注 | 当前列的备注信息。 |
| 索引 | 索引名 |
| 索引类型 | 索引类型包括 Primary、Normal、Unique 三种类型。 |
| 包含列 | 包含列可编辑（支持新增和删除）。 |
| 发布库 | 新增库 |
| 从库组导入 | 在选择库组导入弹窗中，选择已创建的库组，单击确定。
说明：如果未创建库组或不符合业务要求，可单击右上角的管理库组，创建库组或修改库组，详情请参见库组管理。 |


#### 方法二：SQL 导入

1在可视化建表页面左上方，单击 SQL 导入，进入 SQL 导入页面。2在 SQL 导入页面，输入 Create 语句，单击导入，将 SQL 解析至可视化页面。3在新建表页面，单击提交，弹出风险检测结果。○若无报错，可单击确认提交，则可成功建表。○若有注意点，则需单击知晓注意，继续提交，跳转到提交 SQL 预览弹窗。
在提交 SQL 预览弹窗，单击确认提交。
风险检测点如下：
| 检测类型 | 风险检测点 |
| --- | --- |
| 表头 | 禁止表名为关键字 |
| 表必须填写备注 |  |
| 主键&索引 | 必须要有主键（PK） |
| 禁止索引名为关键字 |  |
| 禁止索引名大写 |  |
| 控制 Unique 索引名格式 |  |
| 控制 Normal 索引名格式 |  |
4提交后将自动跳转到操作日志页面，等待执行完成。

#### 方法三：SQL 窗口操作

在数据库管理页面，左侧库表资源区域选择要新建表的数据库或表分组，右键单击数据库打开功能菜单，选择 SQL 操作，打开 SQL操作页面，输入建表语句。

## 相关文档

●删除表●编辑表​
