# 静态站点

返回文档云开发产品支持静态托管能力，您可以在此发布自己的站点并支持自定义域名、缓存管理、防盗连接、IP 黑白名单和跨域访问管理等核心能力。

## 创建文件夹

1登录云开发平台

。2在左侧导航栏选择数据存储 > 静态站点。3在存储管理页签单击新建文件夹。4在新建文件夹弹窗中，输入文件夹名称，单击确定。
文件夹命名限制如下：●不能为空。●不能以正斜线（/）开头。●文件夹名称最多支持255个字符。●推荐使用大小写英文字母、数字，即[a-z，A-Z，0-9]和符号-、!、_、.、*及其组合。●不支持 ASCII 控制字符中的字符上（↑），字符下（↓），字符右（→），字符左（←），分别对应 CAN(24)、EM(25)、SUB(26)、ESC(27)。●如果用户上传的文件或文件夹的名字带有中文，在访问和请求这个文件或文件夹时，中文部分将按照 URL Encode 规则转化为百分号编码。●不建议使用的特殊字符:`^ " \ {}[]~%#\><及 ASCII 128~255 十进制。●可能需特殊处理后再使用的特殊字符:, : ;=&$@+?空格及ASCII 字符范围：00-1F 十六进制（0-31 十进制）以及7F（127 十进制）。

## 上传文件

在存储管理页签下，单击右上角上传文件，选择上传文件或上传文件夹进行上传。说明：●支持上传文件和上传文件夹。●支持同时上传多个文件及文件夹。●文件名称不得包含/。●文件名长度不得超过255个字符。●文件大小不能超过100 MB。

## 查看文件详情

在存储管理页签文件列表中，单击操作列的查看详情， 弹出文件详情弹窗，您可以在此可查看文件大小、格式、ETag、更新时间、储存位置、下载地址和 FileID。

## 查看文件路径

在存储管理页签下单击文件名称，在文件列表上方会显示相应的文件路径。单击文件路径名支持快速切换到目标路径，也可以使用右侧返回上级目录。

## 删除文件

在存储管理页签下，勾选对应文件名，单击批量删除。

## 缓存刷新

缓存刷新支持按文件和文件夹刷新。1在存储管理页签下，单击右上角缓存刷新。2在缓存刷新弹窗中，选择缓存刷新类型，并输入文件或文件夹路径单击确定。注意：●文件夹限制每天仅刷新1次，预计5分钟左右全站刷新。●文件限制每天可刷新20次，预计5分钟左右全站刷新。●单文件的路径不以斜杠（/）结尾。●文件夹以斜杠（/）结尾。

## 静态站点配置

#### 注意

●默认站点的根目录不允许修改，默认为：/ ， 默认站点不允许删除。●每个站点都可以独立配置索引、404页面、域名、缓存、防盗链等配置。 ●静态站点必须要设置索引文件，否则请求站点会出现报错。

### 站点信息

静态站点默认单个环境只有一个站点（默认站点），如果需要使用多站点能力，目前可采用绑定根目录方式实现逻辑隔离的多站点。1在静态站点配置页签，单击右侧的添加站点。
2在添加站点弹窗中，输入站点名称后单击确定。说明：站点名称支持1~30个字符。3站点添加完成后，在静态站点配置页签下单击新建的站点进行切换。
4切换到对应的站点后，单击站点信息右侧的编辑，可配置站点名称、站点目录、索引文件和404页面。如需删除相应的站点，可单击右侧的删除站点，在确认弹窗中单击确定，删除即可。
5配置完成后，单击确定。

#### 站点配置示例

每个站点创建时须绑定站点目录，访问域名通过自动拼接根目录以实现不同站点的隔离。例如：●自定义域名： test.alipay.com。●站点目录： /site。●索引文件： index.html。用户在访问自定义域名 https://test.alipay.com

时 , 站点会自动转化为： https://test.alipay.com/site/index.html

。

#### 索引文件

静态站点默认索引文件为 index.html，当访问站点时会直接路由到资源目录中的索引文件。索引文件同时支持自定义路径，默认访问量会按域名+索引路径/索引文件进行访问，您可通过以下操作配置索引文件。1在静态站点配置页签，切换到对应的站点，单击站点信息右侧的编辑。2在编辑页面，配置索引文件后单击确定。
说明：如上图所示，当用户访问env-00jwrXXXXXX-static.normal.cloudstatic.cn时，URL 实际路径为：https://env-00jwrxXXXXXX-static.normal.cloudstatic.cn/dist/index.html

。

#### 404重定向页面

静态站点支持设置404页面重定向转发，当访问站点未匹配到资源时出现 HTTP 404错误时，此功能支持用户自定义配置转发的地址，同时会将 HTTP 转为200，当成正常的请求。1在静态站点配置页签，切换到对应的站点，单击站点信息右侧的编辑。2在编辑页面，配置404页面，单击确定。

### 域名配置

注意：●默认站点将提供1个默认的域名，可以直接使用，用于开发测试和产品体验。●默认域名不支持修改和删除。如果需要自定义域名，可以在站点上请添加相应的域名。

#### 自定义域名

自定义域名支持 HTTP 和 HTTPS 两种类型，如需新增域名请在域名配置右侧单击添加域名。自定义域名配置注意事项如下：●域名初始化中，预计5~10分钟。●自定义域名需要校验阿里云是否备案，如域名未在阿里云进行备案，可前往阿里云进行网站备案

。●域名存在两种黑名单限制，小程序云黑名单和阿里云友商黑名单。小程序云默认域名黑名单：○cloudstatic.cn○cloudbaseapp.cn○alipay.com○alipay.net ●域名新增完成后，会生成一条 CNAME 记录，需要您自行到 DNS 服务提供商上添加 CNAME 记录。如果您的 DNS 服务商是阿里云，请参见阿里云配置方法

。●域名新增录入后需要分发至 CDN 服务器，整体耗时约8~10分钟。●HTTPS 域名需要上传证书公私钥，证书支持 PEM 格式，证书格式说明请参见常见 SSL 证书都有哪些格式？

。○上传公钥时，首尾加上-----BEGIN CERTIFICATE-----和-----END CERTIFICATE-----，否者会提示格式错误。○上传私钥时，首尾加上-----BEGIN RSA PRIVATE KEY-----和-----END RSA PRIVATE KEY-----，否者会提示格式错误。

### 缓存配置

针对文件的 CDN 回源时间进行管理，支持文件类型和文件夹两种匹配方式。1在缓存配置区域，单击添加缓存配置。2在添加缓存配置弹窗中配置以下参数后单击确定。
配置项说明如下：●缓存类型：支持指定目录、文件后缀两种方式进行缓存设置。○指定目录：需要指定文件目录的路径，且必须是以正斜杠（/）开头的文件夹。 支持多个目录配置，每项用半角分号（,）隔开，内容区分大小写。○文件后缀：支持配置多个文件后缀，每项用半角分号（;）隔开，内容区分大小写。●缓存时间：○缓存时间设置最小值不能低于1分钟。○对象存储和静态站点的默认缓存时间为1小时。更新文件后，不会立即生效，需要等待1小时，才可以下载到新文件。若您需要尽快生效，可以增加缓存配置，设置缓存时间为1分钟，缓存配置的生效周期为5分钟。即首次配置缓存1分钟后，此配置会在5分钟后生效。○缓存时间最小单位为分钟，缓存时间最小值为1分钟（不能为0），缓存时间最大值不能超过365天。设置一分钟后，再次更新文件，文件会在一分钟左右更新。因为文件边缘节点回源需要时间，可能会大于一分钟，文件大小会影响回源速度。●注意事项：○过短的缓存时间将大幅度消耗 CDN 流量，建议在使用时合理配置缓存时间。○缓存配置针对文件的 CDN 回源时间进行管理，支持文件类型和文件夹两种匹配方式。默认所有文件的回源时间为两分钟，过短的缓存时间将大幅度消耗 CDN 流量，建议在开发时合理配置缓存时间。

### 编辑缓存

1在静态站点配置页签缓存配置区域，操作列单击配置。2在缓存配置弹窗中您可以根据实际要求更改缓存时间。

### 删除缓存

1在静态站点配置页签缓存配置区域，操作列单击删除。2在二次确认弹窗中单击确定。

### 防盗链配置

为了避免 URL 被恶意使用盗刷公网流量或者盗用资源，可以根据业务需要合理配置域名、IP 来进行安全防护。打开防盗链配置开关，会自动弹出上述窗口，填写配置信息后单击确定，即操作完成，配置项说明如下：●Referer 黑名单： 拒绝 Referer 名单内的域名访问，若 Referer 名单内的域名访问地址，则返回403。●Referer 白名单： 允许 Referer 名单内的域名访问，若 Referer 名单外的域名访问存，则返回403。●Referer： 支持设置最多400条数据，每条一行；支持域名、IP 和通配符*等形式的地址。●允许空 Referer： HTTP请求中 header 为空 Referer（即不带 Referer 字段或 Referer 字段为空）时，如果勾选则将此类情况剔出黑/白名单。

#### 配置示例

●配置www.alipay.com：可限制以www.alipay.com为前缀的所有地址。●配置带端口的域名和 IP，例如www.alipay.com:80、10.10.10.10:80等地址。●配置通配域名*.alipay.com：可限制所有匹配的地址（含路径），如test.alipay.com/123、cloud.alipay.com。

#### 修改防盗链

配置操作完成后，如果需修改请单击编辑。注意：关闭开关再打开后原有的配置将会清除。

### IP 黑白名单配置

IP 黑白名单主要对访问来源进行控制，通过配置请求端 IP 可以有效限制访问来源，阻拦恶意 IP 盗刷、攻击等问题。打开 IP 黑名单配置开关，会自动弹出上述窗口，填写配置信息后单击确定，即操作完成，配置项说明如下：●IP 黑名单 ：用户端 IP 匹配黑名单中的 IP 或 IP 段时，访问 CDN 节点时将直接返回403状态码。●IP 白名单 ：用户端 IP 未匹配白名单中的 IP 或 IP 段时，访问 CDN 节点时将直接返回403状态码。●IP 输入 ：最多可输入50个 IP、地址段；不支持IP:端口形式，支持 IPv6 地址。

### 跨域访问配置

打开跨域访问配置开关填入业务域名，单击确定保存后，该域名即可使用静态资源而没有跨域问题。说明：支持配置200个跨域域名，配置多个域名时请使用英文逗号（,）分隔。

### 图片处理

图像处理开启后，默认使用自适应 WEBP 格式转换功能，在 CDN 访问中可有效压缩部分流量，以此节省 CDN 流量费用。说明：此功能不改变文件大小，即存储容量还是原有文件大小，仅对 CDN 访问自动转化格式以达到压缩目的。打开图片处理开关，会自动弹出上述窗口，填写配置信息后单击确定。配置项说明如下：●支持转换的图片类型： JPEG、JPG、PNG、WEBP、BMP、TIFF、JPEG 2000，以上格式支持转化成 WEBP。说明：图片类型支持25%的 CDN 流量压缩。●自适应 WEBP：开启后，可将其它格式图片自动转换为 WEBP 格式。图片处理使用限制如下：●原图大小不能超过10MB。●原图的宽*高不能超过16,777,216 px。注意：如果原格式为动图，转化为 WEBP 时会转化为静态图片。

## 一键部署

将相应的资源文件拖动至上传区域后，部署静态站点。说明：静态站点的文件格式必须为 ZIP。

## 相关文档

如需免鉴权调用 Mongo DB 数据库，请参见数据库（NoSQL）。​
