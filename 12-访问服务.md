# 访问服务

返回文档云函数支持 HTTP 请求方式和公共网关方式进行函数调用，若要使用 HTTP 访问函数，则需要在访问服务中配置域名和路由信息。

## 注意事项

在小程序端或者其它端进行访问的时候，请使用 HTTPS 协议。如使用 HTTP 域名的 endpoint 请求小程序将无法上线。

## 代码示例


```Plain Text
my.request({
url:"https://+域名+触发路径",
method: GET/POST,
data: {
参数
},
success: (res) => {
resolve(res);
},
fail: (err) => {
reject(err);
}
})
```


## 访问服务设置

1登录云开发平台

。2在左侧导航栏选择环境管理 > 访问服务。3在 HTTP 访问服务页面配置如下信息。1
| 功能 | 说明 |
| --- | --- |
| 启用 HTTP 服务 | 请根据需求开启或关闭服务。若关闭 HTTP 访问，将不能通过 HTTP 请求访问函数。 |
| 默认域名 | 每一个环境提供一个默认域名，可以用于测试访问使用。 |
| 自定义域名 | 配置自定义域名后，若在生产环境，建议您使用自己的域名进行访问，详情请参见添加自定义域名。 |


## 添加自定义域名

1在 HTTP 访问服务页面，单击添加自定义域名。2在添加自定义域名弹窗中配置如下内容后，单击确定。
| 字段 | 说明 |
| --- | --- |
| 域名 | 如无域名，可前往阿里云注册域名

画板







。注意：在绑定自定义域名前，需要先办理网站备案，详情请参见 ICP 备案

画板







。 |
| CNAME | 复制 CNAME 后，需前往 DNS 服务提供商添加 CNAME 记录，将域名指向服务的 CNAME 域名，详情请参见添加解析记录

画板







。 |
| 证书 | ●通常证书是以 .crt 或 .pem 等为扩展名的文件，请使用相应文本编辑器打开证书文件。●证书格式以“-----BEGIN CERTIFICATE-----”开头，以“-----END CERTIFICATE-----”结尾。 |
| 私钥 | ●通常私钥是以 .key 或 .pem 等为扩展名的文件，请使用相应文本编辑器打开私钥文件。●私钥格式以“-----BEGIN (RSA) PRIVATE KEY-----”开头，以“-----END(RSA) PRIVATE KEY-----”结尾。 |
注意：●在添加自定义域名前，需要先办理网站备案，请参见 ICP 备案

。●在添加自定义域名前，还需配置该域名 CNAME 请参见 CNAME 解析

。

## 新建函数路由

函数路由可以用于配置函数的访问路径。1在 HTTP 访问服务页面，函数路由区域单击新建路由。2根据实际需求在新建路由弹窗内配置后，单击确定。
| 参数 | 说明 |
| --- | --- |
| 域名 | 选择路由使用的域名。 |
| 路径 | 函数的访问路径。说明：请以/符号开头，以字母和数字结尾，支持大小写字母、数字、中划线和下划线。 |
| 函数 | 通过此路径调用的函数。 |
| 秘钥鉴权 | 开关开启后，路由需要环境管理中的 AK/SK 鉴权。 |


## 相关文档

●如需查看、新增环境或切换环境，请参见管理环境。●如需查看环境中各资源的使用详情，请参见环境概览。●如需配置云函数防盗刷，请参见安全配置。●如需配置开发接口服务，请参见云调用。    ​
