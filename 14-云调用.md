# 云调用

返回文档云调用功能主要用于开放接口服务，当您开启开放接口服务并在此功能中添加相应接口后，无需维护已添加接口的 cloudbase_accesstoken 和签名，即可免鉴权调用接口，调用方式兼容原有服务端接口。安全可靠，不需使用 HTTPS 协议，性能开销更小。本文为介绍如何在云调用功能中添加和删除开放接口。

## 前提条件

在使用云调用之前，请在控制台中绑定对应产品。若在未绑定产品情况下进行操作将提示无权限，绑定产品请参见配置应用（以小程序码产品为例）。

## 操作步骤

### 添加开放接口

1登录云开发平台

。2在左侧导航栏单击环境管理 > 云调用。3在云调用页面，打开开放接口服务开关，单击添加接口。
| 字段 | 说明 |
| --- | --- |
| 开放接口服务 | 开启后无需维护 cloudbase_accesstoken 和签名，即可免鉴权调用开放接口，调用方式兼容原有服务端接口。无需使用 HTTPS 协议，性能开销减小且安全可靠。 |
| 沙箱调用能力 | 单击展开图标即可配置沙箱调用能力。如果需要测试云调用接口，可以开启沙箱模式进行测试，具体操作请参见快速接入。 |
| 接口密钥授权 | 根据实际需求在添加接口弹窗中配置应用密钥的开放接口，未配置的接口无法使用云调用。 |


### 删除开放接口

在云调用页面找到需要删除的接口后，在操作列单击删除 > 确定。

## 相关文档

●如需查看新增环境或切换环境，请参见管理环境。●如需查看环境中各资源的使用详情，请参见环境概览。●如需进行访问服务相关的配置，请参见访问服务。●如需配置云函数防盗刷，请参见安全配置。●如需查看开放接口请参见云调用 OpenAPI。​
