# 秘钥管理

返回文档在使用云 AI 服务接口前需要在此申请 token，申请后将 token 放置 http 或 nodejs 中即可使用服务，本文介绍申请 token 和使用 token 的操作步骤。

## 新增 Token

1登录云开发平台

，在上方导航栏单击云 AI。2在左侧导航栏选择秘钥管理。3在秘钥管理页面右上角单击生成 Token，API Token 列表将新增一条 Token。4在 API Token 列表描述列单击图标，您可填写相关名称或对 Token 进行描述。单击图标即可保存描述。5在 API Token 列表启用状态列打开开关，该 Token 即可开始使用。

## 使用 Token

新增的 Token 您可以在 AI 服务集 > 服务接口中使用。

## 复制 Token

在 API Token 列表 token 列单击复制图标即可复制 token 信息。

## Token 状态

在 API Token 列表启用状态列根据需求开启或关闭 token。

## 删除 Token 删除

在 API Token 列表操作列单击删除，即可删除目标 token。注意：●删除后不可恢复，请谨慎操作。●删除正在使用的 token。​
