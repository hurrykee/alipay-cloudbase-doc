# 小游戏排行榜

返回文档为方便开发者专注于游戏编写，支付宝小程序云开发提供了排行榜功能的模版接入。开发者只需使用相关的 SDK，即可轻松集成和实现游戏的排行榜功能，无需关注基础能力和存储。本文将介绍排行榜功能的原理，并提供简单的接入 Demo，帮助开发者快速集成。

## 功能架构

●“排行榜”接入功能图示（SDK可集成）●二次开发
当前云开发产品提供函数模板市场，只需要在函数模板市场中安装排行榜模板就可以使用。如果需要进行二次开发，可以使用 VS Code 命令工具进行快速开发、发布。

## 适用场景

●多难度排行榜●自定义维度排行●提供通用排行榜 UI

## 接入 Demo

请参见 Demo

。

## 模板优势

### 面对高峰流量，弹性调度快

●容器资源实时分配改为缓存资源：容器运行依赖资源，例如 IP、下载镜像、挂载 volume、设置 cgroup 和 namespace 等。这些资源分配较慢，基本是分钟级别。而蚂蚁 FaaS 对容器所依赖的所有资源，只要不占用 CPU 和 Mem ，都会对其进行缓存，最终将资源分配的速度做到 0ms。●100ms函数容器冷启动：标准的 OCI 容器的启动方式是 create+ start，启动速度很慢，而蚂蚁 FaaS 采用了 checkpoint+restore 技术进行性能优化。

### 延迟低、响应快

采用蚂蚁自研的 HUSE 容器调度引擎，专门面向高吞吐、低延迟、低成本、急速启动的 Serverleess 场景而设计的。性能上，可以看到 HUSE 容器调度引擎的性能数据，在全集群1万 QPS 吞吐压力下，整个 HUSE 的调度耗时 P50 基本上在 21ms 左右，P99 在 50ms 以内。相比于传统的容器调度引擎，有着数量级级别的提升。

## 安装模板

安装模板请参见将函数模板安装至环境。​
