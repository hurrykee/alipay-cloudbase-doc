# 快速入门

返回文档云开发产品支持 MySQL 数据库，您可以在云函数中免鉴权调用 MySQL 数据库服务。

## 步骤一：开通 MySQL 服务

由于 MySQL 服务不在包年包月套餐内，系统需要对其进行单独收费，收费标准请参见扩展服务付费。1登录云开发平台

。2在左侧导航栏单击扩展服务 > 服务列表。3在服务列表页面，单击 MySQL 卡片的开通。

## 步骤二：在已开通的 MySQL 服务中建表

1在已开通服务区域单击 MySQL 卡片。2在服务信息页面，单击数据库管理。3在库表资源内找到业务库后，在业务库内新建表。请参见新建表。

## 步骤三：访问 MySQL

在云函数中的调用示例：说明：以下示例为 query 免鉴权方法的使用方式，更多方法请参考 serverless-mysql

。
```JavaScript
const cloud = require('@alipay/faas-server-sdk');
cloud.init({});
// ***************
// 免鉴权连接mysql
// ***************
mysql = cloud.mysql();

exports.main = async (event, context) => {

let res = await mysql.query("select * from your_test_table limit 1"); # 此处需要将 "your_test_table limit 1" 替换成真实的数据表。
return {"result": res}

};
```
​
