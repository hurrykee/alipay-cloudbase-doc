# Redis（缓存服务）

返回文档云开发产品支持 Redis 缓存服务，可以在云函数中通过 SDK 使用。

## 开通 Redis 服务

由于 Redis 服务不在包年包月套餐内，系统需要对其进行单独收费，收费标准请参见扩展服务付费。1登录云开发平台

。2在左侧导航栏单击扩展服务 > 服务列表。3在服务列表页面 Redis 卡片单击开通。4在 Redis 弹窗确认计费项并添加参数后单击新建。
| 参数 | 说明 |
| --- | --- |
| 计费项 | 内存容量单击0.15元 /（GB*小时）。 |
| 实例名称 | 请自定义实例名称。 |
| 架构类型 | ●集群版：分片集群架构，支持扩缩容、规格调整。●标准版：标准的 master-slave 架构，支持规格调整。●极简版：128M 内存单副本，免费一年。 |
| 分片数 | 请根据实际需求选择数据节点数量。 |
| 节点类型 | ●单副本：数据节点中只有一个主数据节点，数据可靠性低。●双副本：数据节点中具有主从两个数据节点，数据可靠性高。 |
| 实例规格 | 每个数据节点的内存大小。 |
| 密码 | 请输入密码。 |
| 确认密码 | 请再次输入密码。 |


## 管理已开通的 Redis

### 开启外网访问

1在已开通服务区域单击对应卡片。2在 Redis 详情页面，单击开启外网访问。3在开启外网访问弹窗配置 IP 后单击确定。

### 查删数据

1在 Redis 详情页面，单击查删数据。2在查删数据页面输入 Key 后根据需求单击查询 Key 或删除 Key，处理结果会在查询结果中展示。

### 查看监控

1在 Redis 详情页面，单击查删数据。2在缓存监控页签根据实例、节点和时间查看对应监控。

### 扩缩容

1在 Redis 详情页面，单击更多图标 > 扩缩容。2在扩容缩弹窗配置分片数后单击确定。

### 清除缓存

1在 Redis 详情页面，单击更多图标 > 扩缩容。2在确认弹窗中单击确认。注意：该操作将清空实例所有数据，清空后无法找回。

### 修改密码

在 Redis 详情页面，单击编辑图标输入新密码及确认新密码后，单击确认。

## 相关文档

●如需调用 MySQL 缓存服务，请参见 MySQL。●如需对域名进行备案，请参见 BSN。●如需开通告警通知中的短信通知，请参见 SMS。●如需查看特性和redis兼容性说明，请参见命令支持。​
