# 编辑表

返回文档本主要介绍以编辑表的操作步骤。

## 操作步骤

1在数据库管理页面，左侧库表资源区域选择要编辑的表，右击单击表名打开功能菜单，选择编辑表，打开编辑表页面。
2在编辑表页面，配置表信息。注意：如需将表结构变更同时发布到多个库，请参见以下新增发布库。●只有当前账号的成员角色是账号管理员，或者有本 APP 应用管理员权限时，才可新增发布库。●如发布库下没有同名表将不执行变更。如已存在同名表，将对比表结构执行添加/修改/删除列、添加/删除索引、修改表信息变更。
| 参数 | 说明 |
| --- | --- |
| 基本信息 | 表名 |
| 备注 | 当前表的备注信息，可编辑修改。 |
| 字符集 | 当前表的字符集，不可更改。 |
| 排序规则 | 当前表的排序规则，不可更改。 |
| 自增起始值 | 当前表的自增起始值，不可更改。 |
| 列信息 | 列名 |
| 类型 | 类型共四大类，12 小类。●数值：int、decimal、bit●日期：date、time、year●字符：char、varchar、binary、varbinary●大对象：blob、text原有列不支持更改，新增列不做限制。 |
| 长度 | 不填则取默认值。●原有列不支持更改，新增列不做限制。 |
| 默认值 | 不填则取默认值。●原有列不支持更改，新增列不做限制。 |
| 可空 | 可选择是否可空。●原有列不支持更改，新增列不做限制。 |
| 备注 | 当前列的备注信息。●原有列不支持更改，新增列不做限制。 |
| 索引 | 索引名 |
| 索引类型 | 索引类型包括 Primary、Normal、Unique 三种类型。●原有索引不支持更改，新增索引不做限制。 |
| 包含列 | 包含列不可编辑。●原有索引不支持更改，新增索引不做限制。 |
| 发布库 | 新增库 |
| 从库组导入 | 在选择库组导入弹窗中，选择已创建的库组，单击确定。说明：如果未创建库组或不符合业务要求，可单击右上角的管理库组，创建库组或修改库组，详情请参见库组管理。 |


#### SQL 导入

1单击编辑表页面顶部的 SQL 导入，进入 SQL 导入页面。
2在 SQL 导入页面，直接导入 CREATE 语句，单击导入，将 SQL 解析至可视化页面。
说明：输入 CREATE 语句后，该语句会与原表的 CREATE 语句进行对比，生成 Alter 语句。3在编辑表页面，单击提交，弹出风险检测结果。○若无报错，可单击确认提交。○若有注意点，则需单击知晓注意，继续提交，跳转到提交 SQL 预览弹窗。
在提交 SQL 预览弹窗，单击确认提交。风险监测点如下：
| 检测类型 | 检测风险点 |
| --- | --- |
| 主键&索引 | 控制删除索引。 |
| 字段 | 添加/修改字段，禁止字段名为关键字。 |
| 添加/修改字段，字段名只能由小写字母、数字下划线组成。 |  |
| 修改字段数据类型。 |  |
4提交后将自动跳转到操作日志页面，查看执行进度。

## 相关文档

●新建表●删除表​
