# 库组管理

返回文档支持将多个环境的数据库绑定成一个分组。目前适用于表结构变更的多库发布场景，可在新建表或编辑表时通过从库组导入的方式添加多个库。

## 前提条件

当前账号的成员角色是账号管理员，或者有本 APP 应用管理员权限，才可以新增发布库。

## 新建库组

1在 数据库管理 页面，单击左侧 库表资源区-快捷功能栏。
2在库组管理页面，单击右上角的新建库组。
3在新建库组弹窗中，输入库组名，通过搜索库名，选择对应的数据库，单击确定。

## 编辑库组

1在库组列表中，单击相应库组右侧操作列的编辑库组。
2在编辑库组弹窗中，修改库组名，重新选择相应的数据库，完成后单击确定。

## 删除库组

1在库组列表中，单击相应库组右侧操作列的删除。
2在确认弹窗中单击确定。

## 查看库组详情

通过执行以下操作，查看已创建的库组中对应的数据库信息，包括实例 ID、环境 ID 和 AppID。1在库组列表中，单击库组名。
2在库组详情弹窗中，可查看已创建的库组中对应的数据库信息。

## 库组的应用

在表结构变更的多库发布场景中，在新建表或编辑表时，可选择从库组导入的方式添加多个库。1在新建表或者编辑表页面发布库区域，单击从库组导入。
2在选择库组导入弹窗中，选择已创建的库组，单击确定，即可从库组导入的方式添加多个库。
​
