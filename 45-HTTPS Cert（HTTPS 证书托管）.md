# HTTPS Cert（HTTPS 证书托管）

返回文档域名中的 HTTPS 证书会在特定周期后失效，需要手动重新生成。HTTPS 证书提供自动监测与更新功能，当证书接近有效期时，将自动执行域名验证并生成新证书，全程无需手动操作。

## 前提条件

●证书域名类型为函数自定义域名，请在环境管理 > 访问服务中添加自定义域名。请参见访问服务。●证书域名类型为静态站点自定义域名，请在数据服务 > 静态站点 > 静态站点配置中添加域名。请参见静态站点。

## 开通 HTTPS Cert 服务

1登录云开发平台

。2在左侧导航栏单击扩展服务 > 服务列表。3在更多扩展服务区域 HTTPS Cert 卡片右下角单击开通。

## 新增证书

1在已开通服务区域单击HTTPS Cert 卡片。2在 HTTPS 证书页面单击新增证书。3在新增证书弹窗配置如下信息后单击确定。
| 参数 | 说明 |
| --- | --- |
| 证书名称 | 根据实际情况自定义证书名称。建议使用英文字母、英文句号、数字、下划线（_）和短划线（-）。注意：证书名称不能与已有证书名称重复。 |
| 域名类型 | ●函数自定义域名：请选择环境管理 > 访问服务中的自定义域名。●静态站点自定义域名：请选择数据服务 > 静态站点 > 静态站点配置中的域名。 |
| 域名 | 根据域名类型选择对应域名。 |
| 描述 | （非必填）建议输入证书使用场景。 |
| 托管 | 根据需求打开托管开关，开启后到到期自动生成新的证书。 |
| 域名服务商 | 选择托管平台后输入对应 key 后台将自动验证域名更新证书。●阿里云：请参见创建 AccessKey

画板







。○Ali_Key：aliyun ram key.○Ali_Secret：aliyun ram secret.●CloudFlare：○CF_Email：cloudflare email.○CF_Key：global API key.●腾讯云（DNSPod）：○DP_Id：DNSPod id.○DP_key：DNSPod key.●GoDaddy：○GD_Key：GoDaddy Key.○GD_Secret：GoDaddy Secret. |


## 更新记录

在 HTTPS 证书列表任一证书操作列单击更新记录，即可查看对应更新时间的更新操作记录。

## 删除

在 HTTPS 证书列表任一证书操作列单击删除。注意：删除后无法恢复，请谨慎操作。​
