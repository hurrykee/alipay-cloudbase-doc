# 导出表结构

返回文档在数据库管理中导出多表结构和单表结构。

## 多表结构导出

在数据库管理页面，左侧库表资源区域选择要导出结构的数据库，右键单击库名打开功能菜单，选择导出表结构，打开导出表结构弹框。勾选您要导出的表，单击确定。参数说明如下：
| 参数 | 说明 |
| --- | --- |
| 兼容 MySQL 语法 | 开启语法兼容将自动去掉 SQL中 OB 专属参数部分，例如 ROW_FORMAT、BLOCK_SIZE 等。 |
| 选择表 | 支持单选、多选、全选。 |


## 单表结构导出

在数据库管理页面，左侧库表资源区域选择要导出结构的表，右键单击表名打开功能菜单，选择导出表结构，打开导出表结构弹框。核对导出表结构信息，单击确定。​
