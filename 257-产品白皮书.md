# 产品白皮书

返回文档支付宝云开发平台是基于 Serverless 一站式小程序后端服务开发平台，动态扩缩容低投入，前后端一体化高效研发。●Serverless：提供云函数、云数据库、云存储、静态托管，Redis、MySQL 等全备开发套件，开发者无需关心服务器和底层设施运维，专注于代码逻辑和业务本身。●成本节约：实时弹性伸缩，轻松应对突发流量，按需付费灵活成本管理，节约起步成本。●产品入口：云开发控制台

## 产品介绍

### 产品优势

云开发作为一款 Serverless 产品，具有开箱即用，极简研发特点。●统一技术栈: 小程序前后端同为 JavaScript 语言，前后端统一技术栈，便于全栈研发。●IDE一站式开发: 小程序的前后端开发、测试、发布、部署都可以在 IDE 端中完成，免登陆、免鉴权，单一平台。●免域名注册备案: 小程序前端 SDK 直接调用 Function，无需域名注册备案，方便使用。●运维自动托管: 云开发监控、运维直接自托管，自动拉起。●动态扩缩容: 按需付费，后端资源根据业务流量自动扩容，节省用户成本以及平台资源成本。●纯业务开发：纯业务开发没有应用框架学习成本。●灵活付费方式：提供套餐付费+按需付费+CDN 流量包灵活付费方式最大程度降低用户成本。

### 典型场景

小程序开发：云开发为开发者提供了全备云端小程序开发环境，IDE 内前后端一站式开发 ，支持多端小程序，用户无需关心运维、服务器信息、快速迭代极速上线。H5 应用：云开发 SDK 同样可以在 H5 应用中使用，简化 H5 后端服务调用，用户可以快速开发 H5 应用。小游戏：云开发可以和主流的游戏框架如 Cocos 框架无缝集成，作为小游戏的后端服务框架，简化开发流程，提高小游戏开发效率。

### 功能介绍

#### 函数服务

云开发提供了云函数完整生命周期管理，包含云端 IDE 开发、云端测试、运行环境配置，触发器管理，通过版本管理可以进行灰度流量发布，可观测提供了日志、指标、执行 trace 监控信息。

#### HTTP 访问

函数可以通过 callfunction 免鉴权的方式进行函数调用，同时也可以通过配置自定义域名和访问路由进行 HTTP 访问。

#### 层管理

对于函数中的公共依赖代码可以通过层管理来进行维护，减少代码重复编写，同时可以优化发布、启动速度。

#### NoSQL

云开发提供 MongoDB 支付宝端、原函数端 SDK，用户可以便捷进行数据存储，在控制用户可以进行数据库管理维护操作。

#### 对象存储

支持 OSS 对象存储，具有权限管理和缓存能力，在云函数中和支付宝端提供 SDK，用户可以方便的进行文件上传下载。控制台上可以进行存储文件管理。

#### 监控告警

支持云函数、NoSQL、OSS 核心指标监控告警，可以通过短信、钉钉发送告警通知。

#### 多端开发

云端 IDE 开发云开发支持在线云端 IDE 进行开发，开箱既用没有任何环境依赖。支付宝端 IDE 开发用可以支付宝小程序 IDE 中进行前后端一体开发，开发、测试、发布 IDE 中一栈式开发。

#### CloudPilot 命令开发

用户安装 CloudPilot 后，可以通过命令 方式进行函数的创建，测试、运行、发布，并且可以结合任何的三方 IDE 。创建函数：
```JSON
cpt cloudbase fn create <funcName> [flags]
```
远程调用：
```JSON
cpt cloudbase fn invoke <funcName> [flags]
```
部署函数：
```JSON
cpt cloudbase fn deploy <funcName> [flags]
```
​
