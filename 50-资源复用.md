# 资源复用

返回文档您可以使用资源复用功能将 APPID01 下 ENV 授权给 APPID02 ，在授权完成后 APPID02 和 APPID01可访问同一个 ENV 环境服务。本文为您介绍配置资源复用的操作步骤。

## 前提条件

●请使用云开发产品管理员及以上的角色进行操作。●资源复用支持其它小程序客户端通过免域名 callFunctio 调用本账号的环境服务。

## 操作步骤

### 关联

1登录云开发平台

。2在左侧导航栏单击全局设置 > 全局管理。3在全局管理页面资源复用区域单击关联。
4在添加关联弹窗中填写 AppID。5如需更精准定位请单击添加环境并选择对应环境。6单击确定即可成功关联。

### 解绑

1在资源复用列表，单击对应 APPID 操作列的解绑。2在二次确认弹窗中单击确定。

### 配置

1在资源复用列表，单击对应 APPID 操作列的配置。2在配置弹窗中更改关联环境。

## 云调用授权

资源共享可以让不同的小程序端访问同一个共享环境，如果用户需要共享云调用资源，比如支付接口，可与启用云调用复用，启用云调用复用后被共享人需要在支付宝 APP 上进行确认，授权确认后云调用资源就会被共享。

## 代码示例

API 请参见 my.cloud.createCloudContext。
```JavaScript
const c1 = my.cloud.createCloudContext({
env: '云函数所属 ENV', // 云环境 ID
appId: "云函数所属 APP"
})
// 云环境初始化
await c1.init();
// 调用云托管服务
const res = await c1.callContainer({
path: '/api/count', // 云托管服务路径
method: 'GET', // 按照自己的业务开发，选择对应的请求类型
header: {
"X-Alipay-Service": "云托管服务名称"
}
});
```
​
