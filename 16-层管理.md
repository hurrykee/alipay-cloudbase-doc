# 层管理

返回文档您可以使用层管理功能管理多个有公共依赖业务代码的函数，从而减少编写重复代码，并提高发布和启动函数的速度。本文介绍如何使用层管理功能。

## 新建层

1登录云开发平台

。2在左侧导航栏单击函数服务 > 层管理。3在层管理页面单击新建层。4在新建层弹窗配置相应信息后，单击确认完成层管理的新建。

## 新建版本

您可以在一个层中新建多个版本，一个版本对应一套代码。1在新建层页面单击对应层名称。2在版本管理页面右上角单击新建版本。3在新建版本弹框配置相应信息后，单击确定完成版本的新建。
| 参数 | 说明 |
| --- | --- |
| 版本名称 | 请自定义名称。 |
| 版本描述 | 请根据实际需求输入该版本的描述信息。 |
| 运行环境 | 请根据实际情况选择环境。 |
| 代码 | 上传后缀为 .zip 格式的文件。 |


## 绑定层

您可以根据实际需求将函数代码绑定至对应的层，从而使得代码更加可维护化和模块化，避免对全局函数和变量的依赖，提高代码的复用性。1在左侧导航栏单击函数服务 > 函数列表2在函数列表页面单击对应的函数名称。3在层管理页签单击绑定层。4根据需求配置相应信息后，单击确定完成绑定。

## 解绑层

1在层管理页签绑定层列表的操作列单击解绑。2在二次确认弹窗中单击确认。

## 目录结构

层代码打包的时候请勿包含根目录。
```JavaScript
// 当前函数所在目录
/.../code
|__ index.js
|__ node_modules
|__ package.json
|__ layer_a
|      	|___ index.js
|       |___ node_modules
|      	|___ package.json
|__ layer_b
|___ index.js
|___ node_modules
|___ package.json
```
请按照我们提供的相对路径导入的方式。
```JavaScript
require('./{层名称}/{层中目录/文件}');

Eg:
require('./layer-a/index.js');
```
​
