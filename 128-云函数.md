# 云函数

返回文档

## callFunction

调用其它云函数。
```
<?php
public function callFunction(CallFunctionParam $param): CallFunctionResult
```


### 请求入参

#### CallFunctionParam


| 字段名 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| name | string | 是 | 无 | 云函数名。 |
| data | any | 是 | 无 | 云函数执行参数。 |
| config | CallFunctionConfig | 否 | 无 | 云函数配置。 |


#### CallFunctionConfig


| 字段名 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| env | string | 否 | 调用者所在的环境 ID | 环境 ID，指定云函数调用所需访问的云环境。 |


### 返回参数

#### CallFunctionResult


| 字段名 | 类型 | 备注 |
| --- | --- | --- |
| data | any | 函数执行成功时返回，云函数执行的结果。 |
| errCode | string | 函数执行失败时返回，错误码。 |
| errMsg | string | 函数执行失败时返回，错误信息。 |


### 示例


```PHP
<?php
use Cloud\Cloud;
use Cloud\CloudFunction\CallFunctionConfig;
use Cloud\CloudFunction\CallFunctionParam;
function main($event, $context)
{
$logger = $context->getLogger();
$cloud = new Cloud();
$config = new CallFunctionConfig();
$funcName = "your_function_name";
$funcInputs = [];
$req = new CallFunctionParam($funcName, $funcInputs, $config);
$logger->info("main", "callFunction request=%s", json_encode($req));
$resp = $cloud->callFunction($req);
$logger->info("main", "callFunction response=%s", json_encode($resp));
return $resp->data;
}
```
​
