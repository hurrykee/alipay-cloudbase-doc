# 导出数据

返回文档本文主要介绍如何导出库数据和表数据。

## 导出库数据

在数据库管理页面，左侧库表资源区域选择要导出数据的数据库，右键单击库名打开功能菜单，选择导出数据，打开导出数据弹框。勾选您要导出的文件格式以及导出对象，单击确定。字段说明如下：
| 字段 | 说明 |
| --- | --- |
| 导出文件 | ●支持导出的数据文件格式包括：.xlsx、.cvs、.txt、.sql。 ●默认选中 .xlsx。 |
| 选择表 | ●支持单选、多选、全选。 |


## 导出表数据

在数据库管理页面，左侧库表资源区域选择要导出数据的表，右键单击表名打开功能菜单，选择导出数据，打开导出数据弹框。勾选您要导出的文件格式，按需填入导出条件，单击确定。 字段说明如下：
| 参数 | 说明 |
| --- | --- |
| 导出文件 | ●支持导出的数据文件格式包括：.xlsx、.cvs、.txt、.sql。 ●默认选中 .xlsx。 |
| 导出条件 | ●支持设置 WHERE 条件导出。●默认 SELECT * FROM table WHERE 1=1。 |
​
