# fn 函数管理

返回文档cpt cloudbase fn 可以帮助用户管理云开发的函数信息。

## fn list 查看函数

列出指定环境下的函数列表。

### 基本用法


```
cpt cloudbase fn list [flags]
```


## 命令行参数


```
Flags:
-a, --all          返回所有的函数列表
--asc          返回结果默认按更新时间降序排列，设置后按升序排列
-h, --help         help for list
-l, --limit int    返回数据长度，默认值为20 (default 20)
-o, --offset int   返回数据偏移量

Global Flags:
-e, --env string   云开发环境名称
```


### 示例

1获取 myEnv 环境下的所有的函数列表。
```JavaScript
cpt cloudbase fn deploy <funcName> [flags]
```
2按更新时间顺序获取 myEnv 环境下的所有的函数列表。
```Bash
Flags:
--create   部署时是否需要在函数在远端不存在时进行创建
-h, --help   help for deploy

Global Flags:
-e, --env string   云开发环境名称
```


## fn create 创建函数

您可以使用 fn create 创建一个云函数。如果 fn create 在包含 cloudbaserc.json 文件的项目目录下执行，会自动将模板代码下载到配置文件 functionRoot 指定的位置中。说明：创建函数后尽管会有默认的函数代码，但是状态默认为未发布，您仍然需要上传并发布自己的函数。

### 基本用法



### 命令行参数



## fn remove 删除函数

您可以使用 fn remove 删除指定函数名称的云函数。

### 基本用法



### 命令行参数



## fn invoke 远端调用

您可以使用 fn invoke 调用已经部署的指定云函数。如果 invoke 在含有 cloudbaserc.json 的项目目录下运行，同时在调用时没有通过 --params 来指定调用参数，那么 invoke 会尝试使用 cloudbaserc.json 中对应函数配置的 params 字段来作为调用参数。

### 基本用法



### 命令行参数



### 示例

1不传参直接调用函数。  2带参数调用函数时，所有的参数会组合成一个字典（dict）或映射（map）传递给调用的函数；以下例子传入的参数为 {"foo1": "bar1", "foo2":"bar2"}。

## fn download 下载函数

您可以使用 fn download 来下载指定的函数代码。fn download 默认会下载到当前文件夹中，如果执行时处于项目文件夹中，它会将代码下载至 cloudbaserc.json 中 functionRoot 字段指定的路径中。

### 基本用法



### 命令行参数



### 示例

下载函数至指定文件夹

## fn deploy

fn deploy 可以部署相应的函数代码，该命令必须在包含 cloudbaserc.json 的项目目录下执行。

### 基本用法



### 命令行参数

​
