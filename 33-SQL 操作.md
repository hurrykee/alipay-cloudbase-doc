# SQL 操作

返回文档SQL 操作功能可以便捷地执行各类 SQL 语句，同时支持对 SQL 查询结果进行导出和可视化编辑等操作。

## 打开 SQL 操作

1同一个库支持打开多个 SQL 操作页面，有三种打开方法：○在数据库管理页面，单击库表资源区域快捷功能。
○在数据库管理初始页面，单击快速开始区域的 SQL 操作。
○在数据库管理页面，左侧库表资源区域找到目标库或表，右键单击表名打开功能菜单，选择 SQL 操作。
2SQL 操作页面可划分为：SQL 执行、常用 SQL 和结果集三个区域。

## SQL执行

### 操作栏


| 功能 | 说明 |
| --- | --- |
| 执行 | 执行 SQL 输入区的内容。●SQL 输入区有选中内容：执行选中部分。●SQL 输入区无选中内容：执行全部。 |
| 优化格式 | 优化 SQL 输入区的内容格式。●SQL 输入区有选中内容：优化选中部分。●SQL 输入区无选中内容：优化全部。 |
| 设置 | 1结果展示。○覆盖：每次执行 SQL 时，会清空之前结果集，并显示 SQL 新结果集。○追加：每次执行 SQL 时，之前结果集将会保留，创建新标签页显示新结果集。2字体大小：小、中、大。3结果集显示行数：200行、500行、1000行。 |
| 库空间 | 展示当前 SQL 操作的库空间，支持切换库空间。 |


### SQL 输入区

填入需要执行的 SQL 语句。说明：每条 SQL 不能超过3MB。

## 常用 SQL

### 新增常用 SQL

1两个入口：○在库表资源区域找到要添加常用 SQL 的目标库，右键单击表名打开功能菜单，将鼠标悬停在常用 SQL，选择新增常用 SQL。打开新增常用 SQL 弹框。
○SQL 操作功能页面右侧 常用 SQL 区域，点击，打开新增常用 SQL 弹框。
2新增常用 SQL 弹框内填入名称和 SQL，单击确定。

### 使用常用 SQL

快速将常用 SQL 内容粘贴至 SQL 输入区有两种方式：1在库表资源区域找到目标库，右击打开功能菜单，将鼠标悬停在常用 SQL，单击常用 SQL。2SQL 操作功能页面右侧常用SQL区域，双击常用 SQL。

### 预览常用 SQL

SQL 操作功能页面右侧常用 SQL 区域，鼠标悬停目标常用 SQL，单击，打开常用 SQL 预览 弹框。

### 编辑常用SQL

SQL 操作功能页面右侧常用 SQL 区域，鼠标悬停目标常用 SQL，点击，打开编辑常用 SQL 弹框。可对名称 和 SQL 内容进行编辑，单击确定即可。

### 删除常用 SQL

SQL 操作功能页面右侧常用 SQL 区域，鼠标悬停目标常用 SQL，点击，在确认框中单击确定。

## 结果集

在执行结果区域您可以查看执行历史、SQL 执行结果和编辑结果集等。
| 参数 | 说明 |
| --- | --- |
| 执行历史 | 可查看本库的 SQL 执行历史。 |
| 执行结果 | 导出结果 |
| 选中结果 | 选中结果支持导出格式：.xlsx，.csv，.txt，.sql |
| 预览SQL | 预览结果集 insert 语句。 |
| 单行详情 | 单击可查看单行详情。 |
| 刷新结果 | 单击将重新执行 SQL 并更新结果。 |
| 开启编辑 | 对结果集数据进行可视化增删改操作。 |


### 执行历史

查看本库的 SQL 执行历史。

### 搜索

支持对结果集过滤搜索。结果集左边的搜索框中输入搜索内容，此时与输入内容模糊匹配的数据都会被高亮显示。您也可以勾选只显示搜索行，此时仅会筛选出与输入内容模糊匹配的数据行。

### 导出结果

支持导出不同格式的结果集和选中行。导出格式支持.xlsx，.csv，.txt，.sql。

### 预览 SQL（insert）

预览结果集 insert 语句。

### 单行详情

分页展示单行详情。

### 刷新结果

重新执行 SQL 并更新结果。

### 开启编辑

对结果集数据进行可视化增删改操作。

#### 新增一行

新增一行数据。

#### 删除一行

删除一行数据。

#### 提交修改

1可视化编辑完成后，单击提交修改。2自动打开提交修改弹框，内容为待执行的变更 SQL。确认无误后单击确认提交。
3自动打开执行结果弹框，可查看每条语句的执行详情。

#### 退出编辑

退出编辑状态，撤销本次所有编辑记录，数据恢复至编辑前。​
